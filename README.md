# kill_PID
If a process / Port (e.g. 5000) is already in use by another python programm (e.g. server_rpi.py)
the process & port can be killed. This report can be run directly on the Rpi.
-> sudo python kill_PID.py 5000
-> enter with yes
